# Python 3.11 or newer.  Recommended: Python 3.14, the current stable release
# (first released Oct 2025, supported to Oct 2030).
#
# Tested: all seven notebooks run end to end on Python 3.14 with
#   numpy 2.5.3, scipy 1.18.1, matplotlib 3.11.1, scikit-learn 1.9.0,
#   jupyterlab 4.6.3
# producing 113 figures with no errors and no deprecation warnings.
#
# The floors below are deliberately loose so the notebooks still install on an
# older interpreter.  Note that numpy >= 2.5 and scipy >= 1.18 now require
# Python >= 3.12, so on 3.11 pip resolves to correspondingly older releases,
# which also work.

numpy>=1.26
scipy>=1.11
matplotlib>=3.8
scikit-learn>=1.4
jupyterlab>=4.2

# Keeps notebook outputs out of git history (see README.md)
nbstripout>=0.8
